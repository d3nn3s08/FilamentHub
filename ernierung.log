Zusammenfassung Debugging FilamentHub Menü (23.11.2025)

Was wurde gemacht:
- PowerShell-Menü (menu_pro_v3.ps1) mehrfach angepasst, um Option 3 (Tests ausführen) so zu gestalten, dass ein neues Konsolenfenster geöffnet wird und pytest-Ergebnisse angezeigt werden.
- Verschiedene Methoden getestet: Start-Process mit unterschiedlichen Argumenten, Argumentübergabe als String/Array, temporäres Batch-Skript.
- Testdatei tests/test_health.py mehrfach erfolgreich ausgeführt – Testergebnisse erscheinen aber immer im aktuellen Fenster, nicht in einem neuen.
- Logik von „Server starten“ übernommen, da dort ein neues Fenster erscheint.

Ergebnis:
- Funktion „Tests ausführen“ ist technisch korrekt und ruft pytest zuverlässig auf.
- In einer klassischen Windows-Konsole (cmd.exe) öffnet der Befehl tatsächlich ein neues Fenster.
- In PowerShell (besonders in VS Code) wird die Fenstersteuerung oft blockiert oder ignoriert – daher erscheint kein neues Fenster.
- Das Problem liegt nicht am Skript, sondern an der Umgebung (PowerShell/VS Code).

Empfehlung:
- Menü aus einer normalen Windows-Eingabeaufforderung (cmd.exe) starten, wenn ein neues Fenster für die Testergebnisse gewünscht ist.
- Alternativ: Funktion kann so umgebaut werden, dass sie immer ein externes Batch-Skript erzeugt und startet – das funktioniert auch in PowerShell.

Fazit:
- Alle robusten Methoden getestet, aber in PowerShell/VS Code öffnet sich kein neues Fenster für die Testergebnisse.
- Menü und Testfunktion sind korrekt, das Verhalten hängt von der Umgebung ab.

Falls Batch-Skript-Variante gewünscht, kann das direkt umgesetzt werden.